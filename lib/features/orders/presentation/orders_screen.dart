import 'package:flutter/material.dart';

/// Placeholder for the orders destination.
///
/// Intentionally empty: M0 builds the shell, not the screens. The AppBar title
/// comes from the shell, so switching locale is visible here already.
class OrdersScreen extends StatelessWidget {
  const OrdersScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.expand();
}
