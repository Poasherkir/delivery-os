<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Nothing this app stores leaves the device.

      The database is encrypted with a non-exportable Android Keystore key, so
      a backup restored onto another device arrives with no key that can ever
      open it. It would surface as an app that starts and cannot read its own
      data. Absent is better than present and unreadable.

      allowBackup covers cloud backup on Android 11 and below;
      dataExtractionRules covers both cloud backup and device-to-device
      transfer on 12 and above, which allowBackup does not.
    -->
    <!--
      The barcode scanner (M1-07), and nothing else. A driver scans the label
      on a parcel; the frames are read for a barcode and discarded, never
      written to storage.

      This is the app's first permission and it is deliberately narrow.
      `android_permissions_test` holds the allowlist, so this line and the one
      beside it there are the review — a permission cannot arrive quietly.

      Not requested, and forbidden by that same test: ACCESS_BACKGROUND_LOCATION.
      Invariant 11 is that background location never arrives, and CAMERA is not
      a step toward it.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!--
          Package visibility for the customer profile's call and WhatsApp
          actions (M1-10). Android 11 and above hide installed apps unless
          they are declared here, and url_launcher's canLaunchUrl answers
          "no" for anything it cannot see — so without these two the buttons
          would be permanently disabled on every modern phone, silently and
          only on a real device.

          Declaring an intent is not requesting a permission and grants no
          access to anything: it lets this app ask whether *something* can
          open a dial or a link, and hand it over. The dialer then shows the
          number and waits for the driver — this app never places a call.
        -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
